swor_result
swor
set.seed(179)
n = 10
swor = sample(1:35, size = n, replace = FALSE)
swor_result = mean(heights$heights[swor])
S = var(heights$heights)
f = 1-n/N
variance = (1-f)*S/n
set.seed(1)
N = 100
heights = rnorm(n = N, mean = 65, sd = 5)
heights = data.frame(heights)
set.seed(179)
n = 10
swor = sample(1:35, size = n, replace = FALSE)
swor_result = mean(heights$heights[swor])
S
S = var(heights$heights)
f = 1-n/N
variance = (1-f)*S/n
S
Y = heights$heights
sum((Y-mean(Y))^2)/(N-1)
knitr::opts_chunk$set(echo = FALSE)
variance
set.seed(198123)
n = 10; num.iters = 10000
swor_results = vector(length = num.iters)
for (i in 1:num.iters) {
swor = sample(1:35, size = n, replace = FALSE)
swor_results[i] = mean(heights$heights[swor])
}
mean((swor_results - mean(heights$heights))^2)
(1-f)*S/n
plot(swor_results)
swor
heights$heights[swor]
heights$heights[swor]
heights$heights[swor]
mean(heights$heights[swor])
set.seed(198123)
n = 10; num.iters = 10000
swor_results = vector(length = num.iters)
for (i in 1:num.iters) {
swor = sample(1:35, size = n, replace = FALSE)
swor_results[i] = mean(heights$heights[swor])
}
swor_results
var(swor_results)
var(Y)
var(Y)/n
var(Y)/n*(1-f)
f
f
f = n/N
variance = (1-f)*S/n
variance
set.seed(198123)
n = 10; num.iters = 10000
swor_results = vector(length = num.iters)
for (i in 1:num.iters) {
swor = sample(1:35, size = n, replace = FALSE)
swor_results[i] = mean(heights$heights[swor])
}
mean((swor_results - mean(heights$heights))^2)
set.seed(1651891)
n = 10; num.iters = 10000
swor_results = vector(length = num.iters)
for (i in 1:num.iters) {
swor = sample(1:35, size = n, replace = FALSE)
swor_results[i] = mean(heights$heights[swor])
}
mean((swor_results - mean(swor_results))^2)
variance
Y = heights$heights
y = heights$heights[swor]
swor_result = mean(y)
S = var(Y)
f = n/N
variance = (1-f)*S/n
est_variance = (1-f)*var(y)
est_variance
est_variance = (1-f)*var(y)/n
variance = (1-f)*S/n
var(swor_results)
(1-f)*S/n
mean((swor_results - mean(swor_results))^2)
variance
mean((swor_results - mean(swor_results))^2)
var(swor_results)
S
s
var(swor_result)
var(swor_results)
var(y)
var(Y)
swor_results = vector(length = num.iters)
var_swor_results = vector(length = num.iters)
for (i in 1:num.iters) {
swor = sample(1:35, size = n, replace = FALSE)
swor_results[i] = mean(heights$heights[swor])
var_swor_results[i] = var(heights$heights[swor])
}
var_swor_results
mean(var_swor_results)
variance
S
y
y > 63
y > 62
